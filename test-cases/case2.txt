function square$ [base:real]
{
	return base * base;
}

%%

 real data1$, data2$;
 get(data1$, data2$);
 data1$ = square$(data1$);
 put(data1$);
 data2$ = square$(data2$);
 put(data2$);
 put(data1$ + data2$);
